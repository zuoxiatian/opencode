# Desktop Vite package notes

- Treat this package as locally maintained code separate from the upstream open-source project.
- When modifying functionality in this package, keep code changes inside `packages/desktop-vite`.
- Do not change other packages, root project files, lockfiles, or shared configuration for this package unless the user explicitly asks for that specific cross-project change.
